name: "Apicurio Registry - Protobuf"
artifact: ${project.groupId}:${project.artifactId}:${project.version}
description: "Use Apicurio Registry for Protobuf serialization/deserialization with Kafka"
metadata:
  keywords:
    - "apicurio"
    - "registry"
    - "protobuf"
    - "kafka"
    - "schema"
    - "serialization"
  categories:
    - "messaging"
    - "serialization"
  status: "experimental"
