name: Publish Platform

on:
  push:
    tags:
      - 'bom-v*'
      - 'devservices-v*'
      - 'apicurio-v*'
      - 'dynamic-grpc-v*'
      - 'service-registration-v*'
      - 'tika-v*'
  workflow_dispatch:
    inputs:
      extension:
        description: 'Extension to publish (or "all")'
        required: true
        type: choice
        options:
          - all
          - bom
          - devservices
          - apicurio
          - dynamic-grpc
          - service-registration
          - tika4-shaded

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Buf
        uses: bufbuild/buf-action@v1
        with:
          token: ${{ secrets.BUF_TOKEN }}
          setup_only: true

      - name: Configure GPG
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
        if: env.GPG_PRIVATE_KEY != ''
        run: |
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
          # Verify GPG key was imported
          gpg --list-secret-keys --keyid-format LONG || (echo "GPG key import failed!" && exit 1)

      - name: Publish pipestream-bom
        if: github.event.inputs.extension == 'bom' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/bom-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          VERSION=$(./gradlew :bom:currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Building BOM version $VERSION"
            ./gradlew :bom:clean :bom:build --no-daemon --stacktrace
            echo "Publishing BOM version $VERSION to Maven Central"
            ./gradlew :bom:publishAllPublicationsToCentralPortal --no-daemon --stacktrace
            echo "Publishing BOM version $VERSION to GitHub Packages"
            ./gradlew :bom:publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ BOM $VERSION published successfully"
          else
            echo "Skipping BOM - version $VERSION is a SNAPSHOT (no changes)"
          fi

      - name: Publish pipestream-quarkus-devservices
        if: github.event.inputs.extension == 'devservices' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/devservices-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          cd pipestream-quarkus-devservices
          VERSION=$(../gradlew currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Publishing devservices version $VERSION to Maven Central"
            ../gradlew clean build publishAggregationToCentralPortal --no-daemon --stacktrace
            echo "Publishing devservices version $VERSION to GitHub Packages"
            ../gradlew publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ devservices $VERSION published successfully"
          else
            echo "Skipping devservices - version $VERSION is a SNAPSHOT (no changes)"
          fi

      - name: Publish quarkus-apicurio-registry-protobuf
        if: github.event.inputs.extension == 'apicurio' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/apicurio-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          cd quarkus-apicurio-registry-protobuf
          VERSION=$(../gradlew currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Publishing apicurio version $VERSION to Maven Central"
            ../gradlew clean build publishAggregationToCentralPortal --no-daemon --stacktrace
            echo "Publishing apicurio version $VERSION to GitHub Packages"
            ../gradlew publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ apicurio $VERSION published successfully"
          else
            echo "Skipping apicurio - version $VERSION is a SNAPSHOT (no changes)"
          fi

      - name: Publish quarkus-dynamic-grpc-extension
        if: github.event.inputs.extension == 'dynamic-grpc' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/dynamic-grpc-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          cd quarkus-dynamic-grpc-extension
          VERSION=$(../gradlew currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Publishing dynamic-grpc version $VERSION to Maven Central"
            ../gradlew clean build publishAggregationToCentralPortal --no-daemon --stacktrace
            echo "Publishing dynamic-grpc version $VERSION to GitHub Packages"
            ../gradlew publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ dynamic-grpc $VERSION published successfully"
          else
            echo "Skipping dynamic-grpc - version $VERSION is a SNAPSHOT (no changes)"
          fi

      - name: Publish pipestream-service-registration-extension
        if: github.event.inputs.extension == 'service-registration' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/service-registration-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          cd pipestream-service-registration-extension
          VERSION=$(../gradlew currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Publishing service-registration version $VERSION to Maven Central"
            ../gradlew clean build publishAggregationToCentralPortal --no-daemon --stacktrace
            echo "Publishing service-registration version $VERSION to GitHub Packages"
            ../gradlew publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ service-registration $VERSION published successfully"
          else
            echo "Skipping service-registration - version $VERSION is a SNAPSHOT (no changes)"
          fi

      - name: Publish tika4-shaded
        if: github.event.inputs.extension == 'tika4-shaded' || github.event.inputs.extension == 'all' || startsWith(github.ref, 'refs/tags/tika-v')
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          cd tika4-shaded
          VERSION=$(../gradlew currentVersion -q | tail -1 | sed 's/Project version: //')
          echo "Version: $VERSION"

          if [[ "$VERSION" != *"SNAPSHOT"* ]]; then
            echo "Publishing tika4-shaded version $VERSION to Maven Central"
            ../gradlew clean build publishAllPublicationsToCentralPortal --no-daemon --stacktrace
            echo "Publishing tika4-shaded version $VERSION to GitHub Packages"
            ../gradlew publishAllPublicationsToGitHubPackagesRepository --no-daemon --stacktrace
            echo "✅ tika4-shaded $VERSION published successfully"
          else
            echo "Skipping tika4-shaded - version $VERSION is a SNAPSHOT (no changes)"
          fi
