name: Create a Release

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Release type (patch=current version, minor/major=bump)'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

      release_all:
        description: 'Release ALL components'
        type: boolean
        default: true

      bom:
        description: 'Release BOM'
        type: boolean
        default: false

      tika4_shaded:
        description: 'Release tika4-shaded'
        type: boolean
        default: false

      devservices:
        description: 'Release devservices'
        type: boolean
        default: false

      apicurio:
        description: 'Release apicurio'
        type: boolean
        default: false

      dynamic_grpc:
        description: 'Release dynamic-grpc'
        type: boolean
        default: false

      service_registration:
        description: 'Release service-registration'
        type: boolean
        default: false

      skip_build:
        description: 'Skip build verification'
        type: boolean
        default: false

      dry_run:
        description: 'Dry run (show versions without tagging)'
        type: boolean
        default: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build verification
        if: inputs.skip_build == false
        env:
          GRADLE_OPTS: -Xmx2048m -XX:MaxMetaspaceSize=512m
        run: |
          echo "Running build verification..."
          ./gradlew buildAll --no-daemon
          echo "Build successful!"

      - name: Calculate versions and create tags
        id: release
        run: |
          # Function to calculate release version
          # - patch: just strip -SNAPSHOT (release current dev version)
          # - minor: bump minor, reset patch
          # - major: bump major, reset minor and patch
          get_release_version() {
            local version=$1
            local bump_type=$2

            # Strip -SNAPSHOT if present
            version=${version%-SNAPSHOT}

            IFS='.' read -r major minor patch <<< "$version"

            case $bump_type in
              major) echo "$((major + 1)).0.0" ;;
              minor) echo "$major.$((minor + 1)).0" ;;
              patch) echo "$major.$minor.$patch" ;;  # Just release the current version
            esac
          }

          # Function to get current version for an extension
          get_version() {
            local ext_dir=$1
            cd "$ext_dir"
            version=$(../gradlew currentVersion -q 2>/dev/null | tail -1 | sed 's/Project version: //')
            cd ..
            echo "$version"
          }

          BUMP_TYPE="${{ inputs.bump_type }}"
          DRY_RUN="${{ inputs.dry_run }}"
          TAGS_TO_CREATE=""

          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Extension | Current Version | New Version | Tag |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|-----------------|-------------|-----|" >> $GITHUB_STEP_SUMMARY

          # Process devservices
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.devservices }}" == "true" ]; then
            CURRENT=$(get_version "pipestream-quarkus-devservices")
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="devservices-v$NEW"
            echo "| devservices | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "devservices: $CURRENT → $NEW"
          fi

          # Process apicurio
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.apicurio }}" == "true" ]; then
            CURRENT=$(get_version "quarkus-apicurio-registry-protobuf")
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="apicurio-v$NEW"
            echo "| apicurio | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "apicurio: $CURRENT → $NEW"
          fi

          # Process dynamic-grpc
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.dynamic_grpc }}" == "true" ]; then
            CURRENT=$(get_version "quarkus-dynamic-grpc-extension")
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="dynamic-grpc-v$NEW"
            echo "| dynamic-grpc | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "dynamic-grpc: $CURRENT → $NEW"
          fi

          # Process service-registration
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.service_registration }}" == "true" ]; then
            CURRENT=$(get_version "pipestream-service-registration-extension")
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="service-registration-v$NEW"
            echo "| service-registration | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "service-registration: $CURRENT → $NEW"
          fi

          # Process tika4-shaded
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.tika4_shaded }}" == "true" ]; then
            CURRENT=$(get_version "tika4-shaded")
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="tika-v$NEW"
            echo "| tika4-shaded | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "tika4-shaded: $CURRENT → $NEW"
          fi

          # Process BOM (different structure - it's a subproject)
          if [ "${{ inputs.release_all }}" == "true" ] || [ "${{ inputs.bom }}" == "true" ]; then
            CURRENT=$(./gradlew :bom:currentVersion -q 2>/dev/null | tail -1 | sed 's/Project version: //')
            NEW=$(get_release_version "$CURRENT" "$BUMP_TYPE")
            TAG="bom-v$NEW"
            echo "| bom | $CURRENT | $NEW | \`$TAG\` |" >> $GITHUB_STEP_SUMMARY
            TAGS_TO_CREATE="$TAGS_TO_CREATE $TAG"
            echo "bom: $CURRENT → $NEW"
          fi

          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$DRY_RUN" == "true" ]; then
            echo "**Dry run - no tags created**" >> $GITHUB_STEP_SUMMARY
            echo "::notice::Dry run complete. No tags were created."
            echo "dry_run=true" >> $GITHUB_OUTPUT
          else
            echo "tags=$TAGS_TO_CREATE" >> $GITHUB_OUTPUT
            echo "dry_run=false" >> $GITHUB_OUTPUT
          fi

      - name: Create and push tags
        if: steps.release.outputs.dry_run == 'false'
        env:
          GH_TOKEN: ${{ secrets.GH_WORKFLOW_TOKEN }}
        run: |
          TAGS="${{ steps.release.outputs.tags }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Use the PAT for pushing (enables workflow triggers)
          git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/ai-pipestream/pipestream-platform.git

          for TAG in $TAGS; do
            echo "Creating tag: $TAG"
            git tag "$TAG"
          done

          echo "Pushing tags..."
          git push --tags

          # Verify tags were pushed
          if [ $? -eq 0 ]; then
            echo "✅ Tags pushed successfully"
            echo "**Tags created and pushed successfully!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "The publish workflow will now trigger automatically for each tag." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Published Tags" >> $GITHUB_STEP_SUMMARY
            for TAG in $TAGS; do
              echo "- \`$TAG\`" >> $GITHUB_STEP_SUMMARY
            done
          else
            echo "❌ Failed to push tags!" >&2
            exit 1
          fi
